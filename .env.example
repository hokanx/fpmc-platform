# FPMC Website — environment template.
# Copy to .env (git-ignored). Never commit real secrets.
#
# Not used by Website V0 (no capture, no auth yet). These are placeholders
# for Block 3 (email capture) — the Supabase anon values are safe to expose
# to the browser but still live in .env so nothing is hardcoded.

# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
